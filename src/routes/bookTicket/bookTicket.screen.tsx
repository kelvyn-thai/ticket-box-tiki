import React from "react";
import styled from "styled-components";

interface IProps {}

const Styled = styled.div``;

const Screen = (props: IProps) => {
  return <Styled className="screen"></Styled>;
};

export default Screen;
