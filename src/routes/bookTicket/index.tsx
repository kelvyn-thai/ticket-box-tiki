import React from "react";
import styled from "styled-components";

interface IProps {}

const Styled = styled.div``;

const BookTicket = (props: IProps) => {
  return <Styled className="book-ticket"></Styled>;
};

export default BookTicket;
